# Final Verification Scan — OAK Healthy

## Security Scan Results

### FIXED (11/14 verified)
1. `.gitignore` covers Firebase configs + `.dbg/` ✅
2. `FirebaseBootstrap.swift` — no hardcoded API keys, reads from plist only ✅
3. `OakPrefs.kt` — EncryptedSharedPreferences with AES-256-GCM ✅
4. 10 Kotlin files migrated to `OakPrefs.get()` ✅
5. `AndroidManifest.xml` — `allowBackup="false"` + `networkSecurityConfig` ✅
6. `network_security_config.xml` — cert pinning with SHA-256 hashes ✅
7. `DebugReporter` gated to `#if DEBUG` with no-op stub in release ✅
8. Update URL allowlist on both iOS and Android ✅
9. `CloudSyncManager.markFailure` uses error code, not localizedDescription ✅
10. 30s rate limit on anonymous Firebase sign-in ✅
11. DEBUG build banners on both platforms ✅

### REMAINING ISSUES (need fix)

| # | Severity | Issue | File:Line | Fix |
|---|----------|-------|-----------|-----|
| S1 | **CRITICAL** | Firebase config files still tracked by git (added to .gitignore AFTER commit) | `iOS/GoogleService-Info.plist`, `Android/app/google-services.json` | `git rm --cached` both files, then rotate Firebase API keys |
| S2 | **HIGH** | iOS cert pinning has placeholder hashes + expired date (2026-01-01) | `CertificatePinning.swift:11-21,24` | Populate real SPKI hashes and update expiration, or remove dead code path |
| S3 | **MEDIUM** | `CloudSyncCrypto.kt` duplicates OakPrefs pattern (creates independent EncryptedSharedPreferences instance for same file — risk of corruption) | `CloudSyncCrypto.kt:37-52` | Replace `getPrefs()` with `OakPrefs.get(context)` |
| S4 | **MEDIUM** | `ActiveClientManager.kt` uses plain SharedPreferences for user profile UUID | `ActiveClientManager.kt:23,61` | Migrate to `OakPrefs.get(context)` |

### NEW ISSUES INTRODUCED
None critical. S3 is the only regression from our changes.

### PASS (no issues found)
SQL injection, command injection, insecure deserialization, NSAllowsArbitraryLoads, exported components, hardcoded secrets — all clean.

---

## UI/UX Scan Results

### FIXED (9/9 verified)
1. `OAKCard.swift` default cornerRadius = 20 ✅
2. `OakCard.kt` default shape = RoundedCornerShape(20.dp) ✅
3. Zero `backgroundGradient` computed properties in iOS views ✅
4. `OakModifiers.swift` with `.oakBackground()` ✅
5. `HomeScreen.kt` uses `SwipeToDismissBox` (Material 3) ✅
6. `SyncCenterScreen.kt` uses `HorizontalDivider` ✅
7. `OakSheets.swift` with shared `OakClientEditorSheet` ✅
8. No private AddClientSheet/ClientEditorSheet definitions remain ✅
9. `HomeView.swift` has `UIImpactFeedbackGenerator` on dose confirm ✅

### REMAINING ISSUES

| # | Severity | Issue | File | Fix |
|---|----------|-------|------|-----|
| U1 | **MEDIUM** | `SettingsScreen.kt` still uses deprecated `Divider()` (7 occurrences) — missed in Material 3 migration | `SettingsScreen.kt:183,363,425,461,491,522,554` | Replace with `HorizontalDivider()` |
| U2 | **LOW** | Android call sites override corner radius to 28dp (GlassCard wrapper, SyncCenterScreen, HistoryScreen, etc.) vs default 20dp | `HomeScreen.kt:684`, `SyncCenterScreen.kt`, `HistoryScreen.kt` | Change overrides to 20dp for parity, or accept as intentional Android style |
| U3 | **LOW** | iOS empty-state card uses `cornerRadius: 16` instead of default 20 | `HomeView.swift:54` | Change to 20 for internal consistency |

### CROSS-PLATFORM CONSISTENCY
| Aspect | iOS | Android | Match? |
|--------|-----|---------|--------|
| OakCard default | 20dp | 20dp | ✅ |
| Background gradient | OakModifiers.swift | OakBackground.kt | ✅ |
| Swipe-to-dismiss | .swipeActions | SwipeToDismissBox | ✅ |
| Haptic feedback | UIImpactFeedbackGenerator | LocalHapticFeedback | ✅ |
| Card override radii | 14-16pt | 28dp | ⚠️ |
| SettingsScreen Divider | N/A (SwiftUI) | Still Material 2 Divider | ⚠️ |

### RECOMMENDATIONS
1. Fix U1 (SettingsScreen Divider) — straightforward, 5 min
2. Fix S1 (git rm --cached Firebase configs) — critical for key rotation
3. Fix S3 (CloudSyncCrypto reuse OakPrefs) — prevents potential corruption
4. S2 (cert pinning hashes) — needs real cert capture, may defer
